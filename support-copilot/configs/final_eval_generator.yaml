mode: full
seed: 42
device: cuda

data:
  max_eval_queries: 1000

routing:
  top_k_domains: 3
  fallback_to_global_search: true
  fallback_score_threshold: 0.75

triage:
  checkpoint: outputs/triage_balanced/distilbert
  reject_threshold: 0.3
  nearest_kb_similarity_threshold: 0.3
  centroid_similarity_threshold: 0.25
  reject_require_lexical_low: true
  ticket_threshold: 0.4

generation:
  enabled: true
  model_name: google/flan-t5-small
  fallback_model_name: google/flan-t5-small
  max_new_tokens: 96
  num_beams: 4
  do_sample: false
  answer_only: true
  insufficient_token: INSUFFICIENT_EVIDENCE

answer_quality:
  enabled: true
  min_answer_words: 6
  block_fragment_answers: true
  strip_inline_citations: true
  deduplicate_citations: true
  reject_vague_queries: true

top_k_retrieval: 20
top_k_rerank: 5
max_rerank_candidates: 15
use_reranker: true
tau_domain: 0.35
tau_chunk: 0.3
fp16: true

# Uses existing retriever/index/routing/triage checkpoints. The small generator
# is loaded from local cache when available; otherwise extractive synthesis is used.
